export const signUp = (state, action) => {
    switch (action.type) {
        case "SIGN_UP":
            return {
                status
            };
        default:
            return state;
    }
};